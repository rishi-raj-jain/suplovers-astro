---
import TourLayout from "../../pageLayouts/ToursLayout.astro";
import { getCollection } from "astro:content";
const tours = await getCollection("tours", ({ id }) => id.startsWith("es/"));
---

<TourLayout tours={tours} indexTitle="Nuestros Tours"/>
